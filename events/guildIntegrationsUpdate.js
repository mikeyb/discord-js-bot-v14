const { Events } = require('discord.js');

module.exports = {
    name: Events.GuildIntegrationsUpdate,
    async execute(guild) {
        // Emitted whenever a guild integration is updated

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing guildIntegrationsUpdate.js');
        //     console.error(error);
        // }
    },
};