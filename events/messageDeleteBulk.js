const { Events } = require('discord.js');

module.exports = {
    name: Events.MessageBulkDelete,
    async execute(messages, channel) {
        // Emitted whenever messages are deleted in bulk.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing messageDeleteBulk.js');
        //     console.error(error);
        // }
    },
};