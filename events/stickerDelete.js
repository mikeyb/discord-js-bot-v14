const { Events } = require('discord.js');

module.exports = {
    name: Events.GuildStickerDelete,
    async execute(sticker) {
        // Emitted whenever a custom sticker is deleted in a guild.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing stickerDelete.js');
        //     console.error(error);
        // }
    },
};