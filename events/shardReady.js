const { Events } = require('discord.js');

module.exports = {
    name: Events.ShardReady,
    async execute(id, unavailableGuilds) {
        // Emitted when a shard turns ready.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing shardReady.js');
        //     console.error(error);
        // }
    },
};