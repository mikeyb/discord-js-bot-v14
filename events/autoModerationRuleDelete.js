const { Events } = require('discord.js');

module.exports = {
    name: Events.AutoModerationRuleDelete,
    async execute(autoModerationRuleDelete) {
        // Emitted whenever an auto moderation rule is deleted.
        // This event requires the PermissionFlagsBits.ManageGuild permission.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing autoModerationRuleDelete.js');
        //     console.error(error);
        // }
    },
};