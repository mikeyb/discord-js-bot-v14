const { Events } = require('discord.js');

module.exports = {
    name: Events.AutoModerationRuleUpdate,
    async execute(oldAutoModerationRule, newAutoModerationRule) {
        // Emitted whenever an auto moderation rule gets updated.
        // This event requires the PermissionFlagsBits.ManageGuild permission.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing autoModerationRuleUpdate.js');
        //     console.error(error);
        // }
    },
};