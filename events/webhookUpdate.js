const { Events } = require('discord.js');

module.exports = {
    name: Events.WebhooksUpdate,
    async execute(channel) {
        // Emitted whenever a channel has its webhooks changed.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing webhookUpdate.js');
        //     console.error(error);
        // }
    },
};