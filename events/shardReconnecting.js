const { Events } = require('discord.js');

module.exports = {
    name: Events.ShardReconnecting,
    async execute(id) {
        // Emitted when a shard is attempting to reconnect or re-identify.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing shardReconnecting.js');
        //     console.error(error);
        // }
    },
};