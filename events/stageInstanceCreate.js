const { Events } = require('discord.js');

module.exports = {
    name: Events.StageInstanceCreate,
    async execute(stageInstance) {
        // Emitted whenever a stage instance is created.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing stageInstanceCreate.js');
        //     console.error(error);
        // }
    },
};