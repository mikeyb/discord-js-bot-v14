const { Events } = require('discord.js');

module.exports = {
    name: Events.MessageCreate,
    async execute(message) {
        // Emitted whenever a message is deleted.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing messageDelete.js');
        //     console.error(error);
        // }
    },
};