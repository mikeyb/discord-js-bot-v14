const { Events } = require('discord.js');

module.exports = {
    name: Events.GuildBanRemove,
    async execute(ban) {
        // Emitted whenever a member is unbanned from a guild.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing guildBanRemove.js');
        //     console.error(error);
        // }
    },
};