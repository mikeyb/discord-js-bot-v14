const { Events } = require('discord.js');

module.exports = {
    name: Events.GuildRoleUpdate,
    async execute(oldRole, newRole) {
        // Emitted whenever a role is updated.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing roleUpdate.js');
        //     console.error(error);
        // }
    },
};