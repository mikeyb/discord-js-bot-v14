const { Events } = require('discord.js');

module.exports = {
    name: Events.GuildEmojiDelete,
    async execute(emoji) {
        // Emitted whenever a custom emoji is deleted in a guild.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing emojiDelete.js');
        //     console.error(error);
        // }
    },
};