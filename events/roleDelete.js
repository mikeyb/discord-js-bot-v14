const { Events } = require('discord.js');

module.exports = {
    name: Events.GuildRoleDelete,
    async execute(role) {
        // Emitted whenever a role is deleted.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing roleDelete.js');
        //     console.error(error);
        // }
    },
};