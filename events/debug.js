const { Events } = require('discord.js');

module.exports = {
    name: Events.Debug,
    async execute(info) {
        // Emitted for general debugging information.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing debug.js');
        //     console.error(error);
        // }
    },
};