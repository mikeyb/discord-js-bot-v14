const { Events } = require('discord.js');

module.exports = {
    name: Events.StageInstanceDelete,
    async execute(stageInstance) {
        // Emitted whenever a stage instance is deleted.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing stageInstanceDelete.js');
        //     console.error(error);
        // }
    },
};