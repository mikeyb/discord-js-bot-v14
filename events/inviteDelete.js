const { Events } = require('discord.js');

module.exports = {
    name: Events.InviteDelete,
    async execute(invite) {
        // Emitted when an invite is deleted.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing inviteDelete.js');
        //     console.error(error);
        // }
    },
};