const { Events } = require('discord.js');

module.exports = {
    name: Events.MessageCreate,
    async execute(message) {
        // Emitted whenever a message is created.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing messageCreate.js');
        //     console.error(error);
        // }
    },
};