const { Events } = require('discord.js');

module.exports = {
    name: Events.ChannelDelete,
    async execute(channel) {
        // Emitted whenever a guild channel is deleted.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing channelDelete.js');
        //     console.error(error);
        // }
    },
};