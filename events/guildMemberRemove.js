const { Events } = require('discord.js');

module.exports = {
    name: Events.GuildMemberRemove,
    async execute(member) {
        // Emitted whenever a member leaves a guild, or is kicked.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing guildMemberRemove.js');
        //     console.error(error);
        // }
    },
};