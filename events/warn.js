const { Events } = require('discord.js');

module.exports = {
    name: Events.Warn,
    async execute(warn) {
        // Emitted for general warnings.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing warn.js');
        //     console.error(error);
        // }
    },
};