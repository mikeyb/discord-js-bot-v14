const { Events } = require('discord.js');

module.exports = {
    name: Events.GuildStickerCreate,
    async execute(sticker) {
        // Emitted whenever a custom sticker is created in a guild.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing stickerCreate.js');
        //     console.error(error);
        // }
    },
};