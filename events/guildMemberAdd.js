const { Events } = require('discord.js');

module.exports = {
    name: Events.GuildMemberAdd,
    async execute(member) {
        // Emitted whenever a user joins a guild.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing guildMemberAdd.js');
        //     console.error(error);
        // }
    },
};