const { Events } = require('discord.js');

module.exports = {
    name: Events.GuildEmojiCreate,
    async execute(emoji) {
        // Emitted whenever a custom emoji is created in a guild.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing emojiCreate.js');
        //     console.error(error);
        // }
    },
};