const { Events } = require('discord.js');

module.exports = {
    name: Events.TypingStart,
    async execute(typing) {
        // Emitted whenever a user starts typing in a channel.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing typing.js');
        //     console.error(error);
        // }
    },
};