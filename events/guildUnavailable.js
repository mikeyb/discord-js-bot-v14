const { Events } = require('discord.js');

module.exports = {
    name: Events.GuildUnavailable,
    async execute(guild) {
        // Emitted whenever a guild becomes unavailable, likely due to a server outage.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing guildUnavailable.js');
        //     console.error(error);
        // }
    },
};