const { Events } = require('discord.js');

module.exports = {
    name: Events.GuildCreate,
    async execute(guild) {
        // Emitted whenever the client joins a guild.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing guildCreate.js');
        //     console.error(error);
        // }
    },
};