const { Events } = require('discord.js');

module.exports = {
    name: Events.GuildMemberAvailable,
    async execute(member) {
        // Emitted whenever a member becomes available.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing guildMemberAvailable.js');
        //     console.error(error);
        // }
    },
};