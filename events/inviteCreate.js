const { Events } = require('discord.js');

module.exports = {
    name: Events.InviteCreate,
    async execute(invite) {
        // Emitted when an invite is created.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing inviteCreate.js');
        //     console.error(error);
        // }
    },
};