const { Events } = require('discord.js');

module.exports = {
    name: Events.GuildStickerUpdate,
    async execute(oldSticker, newSticker) {
        // Emitted whenever a custom sticker is updated in a guild.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing stickerUpdate.js');
        //     console.error(error);
        // }
    },
};