const { Events } = require('discord.js');

module.exports = {
    name: Events.GuildUpdate,
    async execute(oldGuild, newGuild) {
        // Emitted whenever a guild is updated - e.g. name change.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing guildUpdate.js');
        //     console.error(error);
        // }
    },
};