const { Events } = require('discord.js');

module.exports = {
    name: Events.ChannelCreate,
    async execute(channel) {
        // Emitted whenever a guild channel is created.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing channelCreate.js');
        //     console.error(error);
        // }
    },
};