const { Events } = require('discord.js');

module.exports = {
    name: Events.ShardResume,
    async execute(id, relayedEvents) {
        // Emitted when a shard resumes successfully.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing shardResume.js');
        //     console.error(error);
        // }
    },
};