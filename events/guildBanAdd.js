const { Events } = require('discord.js');

module.exports = {
    name: Events.GuildBanAdd,
    async execute(ban) {
        // Emitted whenever a member is banned from a guild.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing guildBanAdd.js');
        //     console.error(error);
        // }
    },
};