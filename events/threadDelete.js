const { Events } = require('discord.js');

module.exports = {
    name: Events.ThreadDelete,
    async execute(thread) {
        // Emitted whenever a thread is deleted.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing threadDelete.js');
        //     console.error(error);
        // }
    },
};