const { Events } = require('discord.js');

module.exports = {
    name: Events.ShardError,
    async execute(event, shardId) {
        // Emitted whenever a shard's WebSocket encounters a connection error.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing shardError.js');
        //     console.error(error);
        // }
    },
};