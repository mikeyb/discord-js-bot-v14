const { Events } = require('discord.js');

module.exports = {
    name: Events.GuildEmojiUpdate,
    async execute(oldEmoji, newEmoji) {
        // Emitted whenever a custom emoji is updated in a guild.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing emojiUpdate.js');
        //     console.error(error);
        // }
    },
};