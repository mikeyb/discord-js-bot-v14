const { Events } = require('discord.js');

module.exports = {
    name: Events.GuildDelete,
    async execute(guild) {
        // Emitted whenever a guild kicks the client or the guild is deleted/left.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing guildDelete.js');
        //     console.error(error);
        // }
    },
};