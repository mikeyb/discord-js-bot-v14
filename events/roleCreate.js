const { Events } = require('discord.js');

module.exports = {
    name: Events.GuildRoleCreate,
    async execute(role) {
        // Emitted whenever a role is created.

        // try {


        // }
        // catch (error) {
        //     console.error('Error executing roleCreate.js');
        //     console.error(error);
        // }
    },
};